###################################################   C++   ##################################################
1.数组名为常量指针，不能作为左值
char* sp, s[10]; s = "Hello";	（错误）

2.math.h的abs返回值()，可能是负数
当num为最小的负数时（即0x80000000），由于正数里int类型32位表示不了这个数的绝对值，所以依然返回该负数。

3.设 char *s="\ta\017bc";则指针变量s指向的字符串所占的字节数是（）。
答案为6："\t"、"a"、"\017"、"b"、"c"、"\0"

4.静态变量，只有初次初始化，是有效的。

5.可作为C语言合法常量的是
C语言中实数常数的科学表示法规定格式为：“实数e整数”或“实数E整数”
其中幂是整数，不能写成实数，e(或E)前后的实数和整数都不能省略掉，
由于C语言规定。0开头的是八进制数，0x(0x)开头的是十六进制数。而八进制数数字是0～7，出现8是错误的

6.static 修饰的变量在类外初始化，const修饰的在参数列表初始化

7.PAT(*ad)[3];
ad首先是个指针；
ad是个指向有着三个PAT元素的数组的指针；
但是并没有实例化PAT

8.友元关系不能被继承，基类的友元对派生类没有特殊的访问权限。

9.不建议在构造函数中抛出异常；
构造函数抛出异常时，析构函数将不会被执行,需要手动的去释放内存

10.INT_MIN;INT_MAX;分别表示整数的最大值和最小值

11.
	基类				public		protect		private
子类:public 基类		public		protect		不可访问成员
子类:protect 基类		protect		protect		不可访问成员				
子类:private 基类		private		private		不可访问成员
protect成员，是指不想被类外部访问到，但是又希望被子类访问到的成员。
派生类对象中含有从基类继承来的成员，但是派生类并不总能直接对其访问和修改

12.
初始化为NULL(0)的类指针可以安全的调用不涉及类成员变量的类成员函数而不出错，但是如果类成员函数中调用了类成员变量则会出错，
（1）调用成员函数的时候，函数地址是编译期间确定的，成员函数不通过对象指针去调用，对象指针仅仅作为参数传入函数然后去调用成员变量。
（2）而如果是虚函数，需要通过this去计算vptr，然后找到vtable，而this为NULL，因此会报错。

13.其中多态性有两种，一种是依靠 函数重载 实现的 静态绑定 ，一种是依靠 虚函数 实现的 动态绑定。

14.
编译器在编译时，不会去判断函数的返回类型，也就是说，你的返回值虽然不同，函数只有调用后，编译器才会去验证类型
所以返回值不能做为重载依据。

15.
枚举类型的sizeof值都是4；
enum只是定义了一个常量集合，里面没有“元素”，而枚举类型是当做int来存储的。
###################################################   C++   ##################################################

--------------------------------------------------------------------------------------------------------------

###################################################	 网络  ###################################################
1.
ARP协议将IP转化为MAC，采用的是广播模式
PARP协议将MAC转化为IP，采用单播模式

2.交换机（多端口的网桥）寻址通过Mac地址，路由器通过目标ip
中继器：将信号放大，传向远方
集线器：实现信号一对多发送

3.QoS 服务质量，是网络的一种安全机制。QoS是【广域网】的一个重要的需求。

4.多址访问协议分类： 
（1）信道划分协议 
【时分多路复用（TDM）】把时间划分为若干个时间帧，把每个时隙分配给N个节点中的一个。
【频分多路复用（FDM）】把信道划分为多个频段，每个节点使用一个频段
【码多分址（CDMA）】为每一个节点分配不同的编码，然后每个节点用它唯一的编码对发送的数据进行编码。这样即使多个节点的信息杂糅在一起也能够让各自的接收方正确接收信息。
（2）随机访问协议：是一个传输节点占用信道全部速率进行发送，当有碰撞时，等待一个随机时延然后重复发送。 
【载波侦听多址访问（CSMA）】先听后发，边发边听，冲突停发，随机延迟后重发 
①其中带碰撞检测的CSMA（CSMA/CD）用于有线局域网如以太网中， 
②而带碰撞避免的CSMA （CSMA/CA）用于无线局域网如802.11中。
【分段（或时隙）ALOHA】边发边听，冲突停发，随机延迟后重发 
我们假定所有帧为L比特，时间被划分为时隙（一个时隙传输一帧）。约定每个结点只在时隙起点开始传输帧，且每个结点都知道时隙何时开始。一旦发生碰撞，就等待一段随机长的时间后重发该帧。
（3）轮流协议：
【轮询协议】
在参加会议的人中选择一个主持人（主节点），由主持人来告诉每一个参加会议的人“你可以讲话了，但是你讲话的量不能超过多少分钟（字节）”
【令牌（Token）传递协议】
令牌环网是一种定义在 IEEE 802.5中，其中所有的工作站都连接到一个环上，每个工作站只能同直接邻的工作站传输数据的网络。

5.
网桥只是利用了mac地址对数据链路层的数据帧进行了转发，对于网络的高层协议而言则是透明的。
因此通过网桥和交换机组成的网络仍属于同一个广播域(在不考虑虚拟网的情况下)，网络中任何一个站点发出的广播数据包都可被其它站点所接收。
因此网桥能够解决信道冲突而不能阻止广播风暴。 :) 路由器能隔离广播风暴

6.以太网络使用CSMA/CD（载波监听多路访问及冲突检测）技术，并以10M/S的速率运行在多种类型的电缆上。

###################################################	 网络  ###################################################

--------------------------------------------------------------------------------------------------------------

#################################################	操作系统   ###############################################
1.
在多线程中，多个线程共享一个进程中的地址空间.
耗时的操作在线程中进行，因为这样并行计算可以防止主界面卡顿。
如果是在多处理器计算机中多个程序可以真正的并行执行，可以提高使用cpu的效率。
线程不能独立执行程序。

2.子进程与父进程的区别在于：
①父进程设置的锁，子进程不继承（因为如果是排它锁，被继承的话，矛盾了）
②各自的进程ID和父进程ID不同
③子进程的未决告警被清除；
④子进程的未决信号集设置为空集

3.非阻塞模式下，表示写入内核socket缓冲区；阻塞模式下表示成功发送出去。

4.线程独有的栈，保存其运行状态和局部自动变量的。栈在线程开始的时候初始化，每个线程的栈互相独立，因此，栈是thread safe的

5.linux命令执行成功后会返回：0（没有消息就是最好的消息）

6.route 查看本机的路由表信息   traceroute 用来探测路由经过

7.当前目录下有a和b两个文件，执行命令“ls>c”，请问文件c里面的内容是什么？  不是ab，是abc

8.死锁的必要条件：
（1）互斥条件：一个资源每次只能被一个进程使用。
（2）请求与保持条件：一个进程因请求资源而阻塞时，对已获得的资源保持不放。
（3）不剥夺条件:进程已获得的资源，在末使用完之前，不能强行剥夺。
（4）循环等待条件:若干进程之间形成一种头尾相接的循环等待资源关系。

9.分段式存储有利于程序的动态链接

#################################################	操作系统   ###############################################

--------------------------------------------------------------------------------------------------------------

#################################################	 数据库    ###############################################
1.
主键是唯一、不为空值的列

2.选择运算尽可能早做，在优化策略中这是最重要，最基本的一条
它常常可以使执行节约几个数量级，因为选择运算一般使计算的中间结果大大变小

3.athletes 表包含运动员姓名，年纪和代表国家（以下，找出代表每个国家最年轻的运动员情况）
SELECT name, country, age FROM athletes WHERE (country, age) IN (SELECT country, min(age) FROM athletes GROUP BY country)
												 ①      ②                ①       ②

4.SQL语言具有的功能：DCL(数据控制语句)、DDL(数据定义语句）、DML(数据操作语句)
#################################################	 数据库    ###############################################


linked-list-cycle
balanced-binary-tree
unique-binary-search-trees	
binary-tree-inorder-traversal
sort-colors
